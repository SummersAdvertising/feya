<section class="content" id="index-show">
  <% if @product %>
  <div class="quick"><p>購物車</p><span></span>件<a href="/orders/cart">結帳</a></div>
  <div style="overflow:hidden">
    <div id="preview"><%= (@product.cover && @product.cover.length > 0) ? image_tag(@product.cover) : "尚未上傳產品封面。" %></div>
    <div id="intro">
      <table width="100%" border="0" cellspacing="0" cellpadding="0">
        <tr>
          <td class="name"><%= @product.name %><br>
            <span class="number">商品編號：<%= @product.id %></span>
          </td>
        </tr>
        <tr>
          <td class="price">
            <% if @product.saleprice %>
            <span class="original">NT$ <%= @product.price %></span><br>
            NT$ <%= @product.saleprice %>
            <% else %>
            NT$ <%= @product.price %>
            <% end %>
          </td>
        </tr>

        <% if !@product.hasType && @product.hasStock %>
        <% if @product.stocks.first.amount && @product.stocks.first.amount >0 %>
        <tr>
          <td class="count">庫存 <%= @product.stocks.first.amount %></td>
        </tr>
        <% end %>
        <% elsif !@product.hasStock %>
        <tr>
          <td class="count">本商品目前無庫存。</td>
        </tr>
        <% end %>

        <% if @product.hasStock %>
        <tr>
          <td class="count">
            訂購數量 <br/>
             <% if @product.hasType %>
             <select id="productType">
              <% @product.stocks.each do |stock| %>
              <% if !stock.amount || stock.amount > 0 %>
              <option value='{"id":<%= stock.id %>, "name":"<%= @product.name %>(<%= stock.typename %>)" }'><%= stock.typename %><%= (": 庫存"+stock.amount.to_s+"個。") if stock.amount %></option>
              <% end %>
              <% end %>
            </select>
            <% end %>

            <input id="addAmount" type="text" />
            
          </td>
        </tr>
        <tr>
          <td class="add"><%= link_to '加入購物車', "#", :id => "addToCart", :data => { :stockid => @product.stocks.first.id, :stockname => @product.name } %></td>
        </tr>
        <% end %>
      </table>
    </div>
  </div>

  <div id="detail">
    <h3>產品說明及注意事項</h3>
    <%= simple_format(@product.description.html_safe) %>
  </div>

  <% else %>
  <p>找不到此商品。</p>
  <% end %>
</section>
<%= render 'orders/cart_js' %>