
  <link rel="stylesheet" type="text/css" href="/peditor/css/peditor.css">
  <script src="/peditor/js/editor.js"></script>
  <script src="/peditor/js/editor_p.js"></script>
  <script src="/peditor/js/editor_img.js"></script>
  <script src="/peditor/js/editor_list.js"></script>
  <script src="/peditor/js/editor_video.js"></script>


<%= form_for([:admin, @testimony]) do |f| %>
  <% if @testimony.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@testimony.errors.count, "error") %> prohibited this admin_testimony from being saved:</h2>

      <ul>
      <% @testimony.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :cover %><br />
    <%= f.text_field :cover %>
  </div>
  <div class="field">
    <%= f.label :status %><br />
    <%= f.text_field :status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
  
<div id="editorPanel"></div>

<hr />
  
	<%= fields_for @testimony.article do | a | %>
	
		<%= a.hidden_field :content, {:id => 'testimony_content'} %>
	
	<% end %>


<section id="articleContent"></section>
  
<% end %>


<script type="text/javascript">
$("#submit").click(function(){
  $(this).parents("form:first").submit();
});


$(document).ready(function(){
	editor.init({ articleModel: 'testimony', photoUpload: '../../peditor/<%= @testimony.article.id %>/createPhoto' });

	$("#submit").click(function(e){
		e.preventDefault();

		if(!editor.isPostNotSent()){
			$("#edit_article_<%= @testimony.article.id %>").submit();
		}
		else{
			editor.alert("段落編輯尚未完成。", "error");
		}
		
	});
});

</script>